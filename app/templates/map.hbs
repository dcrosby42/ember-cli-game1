<h2>Map</h2>

<div id="map-holder">
  <table id="map" class="map">
    {{#each row in map.rows}}
      <tr class="map-row">
        {{#each cell in row.cells}}
          <td class="map-cell">{{cell.text}}</td>
        {{/each}}
      </tr>
    {{/each}}
  </table>
</div>

